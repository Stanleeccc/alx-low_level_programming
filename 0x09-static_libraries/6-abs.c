#include "main.h"

/**
 * _abs - this is used to compute the absolute value of an integer
 * @s: the int to check
 * Return: the absolute value of int
 */

int _abs(int s)
{
	if (s >= 0)
	{
		return (s);
	}
	return (-s);
}
