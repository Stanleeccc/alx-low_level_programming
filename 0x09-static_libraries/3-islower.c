#include "main.h"

/**
 * _islower - this is used to check for lowercase character
 * @c: this represents the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
